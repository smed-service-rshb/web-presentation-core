## Компонент Значок-уведомление

Компонент без параметров. Использовать только совместно с родителем, имеющем relative позиционирование

Пример использования:
```
Example = () =>(
    <Badge>{10}</Badge>
);
<Example />
```